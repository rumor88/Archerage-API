Enabled functions for addons:​
        X2Resident:GetResidentBoardContent(boardType)​
        X2Bag:GetBagItemInfo(bagId, slot)​
        X2Equipment:GetEquippedItemType(equipSlot)​
        X2Equipment:GetEquippedItemTooltipInfo(equipSlot, targetEquippedItem)​
        X2Bag:EquipBagItem(slot, isAuxEquip) - cooldown for use 100ms​
        X2Player:ChangeAppellation(nameType, effectType) - cooldown for use 500ms​

Fixed cooldown (was in seconds instead of milliseconds) for X2Player:ChangeAppellation and X2Bag:EquipBagItem.
Fixed info for X2Bag:GetBagItemInfo.
Additionally enabled functions of X2Player:
   GetUnitAppellationRouteList()
   GetAppellationCount()
   GetAppellations(routeFilter, pageIndex)
   GetAppellationsCount(routeFilter)
   GetShowingAppellation()
   GetEffectAppellation()
   GetAppellationMyLevelInfo()
   GetAppellationBuffInfoByLevels()
   GetAppellationRouteInfo(type)
   GetAppellationStampInfo()
   GetAppellationMyStamp()
   GetAppellationChangeItemInfo()
   GetStampChangeItemInfo()

Addons updates made on 12/24:
 
Fixed a bug when addon reload could crash game.​
Added new uievent on addon loaded "ADDON_LOADED".​
Enabled functions:X2Store:GetSpecialtyRatio()​X2Store:GetZoneSpecialtyRatio()​X2Store:GetSpecialtyRatioBetween(fromZoneGroup, toZoneGroup)​X2Store:GetProductionZoneGroups()​X2Store:GetSellableZoneGroups(fromZoneGroup)​
 
Addons updates made on 1/14:
Fixed X2Bag:GetBagItemInfo crash on some items.​
Fixed X2Map:GetPlayerSextants function​
Enabled enabled X2Unit:UnitGearScore(unit).​
Enabled X2Unit:GetTargetAbilityTemplates(target).​

Enabled functions for addons (to be applied on 1/28):​
enabled X2Unit functions GetTargetUnitId(), GetUnitNameById(stringId), GetUnitId(unit)

Enabled functions (to be applied on 2/25):​
 
X2Item:InfoFromLink(linkText) -- second param can be empty or auction/coffer/guildBank​
X2Resident:RequestHousingTradeList(zoneGroupType, filterindex, searchWord) - answer return via uievent "RESIDENT_HOUSING_TRADE_LIST"​
X2Resident:FilterHousingTradeList(filterindex, searchWord) - answer return via uievent "RESIDENT_HOUSING_TRADE_LIST"​
X2Unit:GetCurrentZoneGroup()​
X2Unit:UnitCastingInfo(unit)​

Enabled functions (to be applied on 3/4):​
X2Team:SetRole(role) - cooldown time 500ms​
X2Team:GetRole(teamIndex, memberIndex)​

Enabled functions (to be applied on 3/11):​
UI:GetServerTimeTable()
X2Time:GetGameTime()
X2Time:GetServerTime()

Enabled functions (to be applied on 3/18):​
X2Map:GetZoneStateInfoByZoneId(zoneId)
X2SiegeWeapon:GetSiegeWeaponSpeed()
X2SiegeWeapon:GetSiegeWeaponTurnSpeed()
X2Skill:GetSkillTooltip(skillId, itemType)
X2Sound:PlayUISound(soundPackItemName)
X2Sound:IsPlaying(soundId)
X2Sound:StopSound(soundId)
X2Sound:PlayMusic(soundPackItemName)
X2Sound:StopMusic()

Update 4/8:
Fixed crash on X2Bag:GetBagItemInfo
Fixed X2Skill:GetSkillTooltip(skillId, itemType) data return
Enabled X2Ability:GetBuffTooltip(buffType, itemLevel)

Enabled functions (to be applied on 4/15):​
    X2Quest:GetActiveQuestListCount()​
    X2Quest:GetActiveQuestType(idx)​
    X2Quest:SetTrackingActiveQuest(idx)​
    X2Unit:GetUnitInfoById(stringId)​


Enabled functions (to be applied on 4/29):​
    X2Ability:GetAllMyActabilityInfos()​
    X2Auction:SearchAuctionArticle(page, minLevel, maxLevel, grade, category, exactMatch, keywords, minDirectPriceStr, maxDirectPriceStr) - cooldown 1sec, table with data received via ui event "AUCTION_ITEM_SEARCHED"​
    X2CombatResource:GetCombatResourceInfo()​
    2CombatResource:GetCombatResourceInfoByGroupType(groupType)​
    X2Craft:GetCraftBaseInfo(craftType)​
    X2Craft:GetCraftMaterialInfo(craftType,doodadId)​
    X2Craft:GetCraftProductInfo(craftType)​
    X2Quest:GetQuestContextMainTitle(type)​
    X2Quest:IsCompleted(type)​


Update 5/6:
-- Enabled functions:​
        X2Auction:GetSearchedItemPage()​
        X2Auction:GetSearchedItemCount()​
        X2Auction:GetSearchedItemTotalCount()​
        X2Auction:GetSearchedItemInfo(idx)​

-- Added a new function to webbrowser RequestExternalPage(url).​

-- Fixed crash on combobox creation.​ 


Updates 6/10:

Added X2Unit:GetUnitWorldPositionByTarget(unit)​
Added function ADDON:GetContentMainScriptPosVis(uiCategory): local x,y,width,height,visible = ADDON:GetContentMainScriptPosVis(uiCategory)​
Added function X2:AddEscMenuButton(categoryId, uiContentType, iconKey, name)​

Enabled functions:​

        -- ADDON:RegisterContentWidget(uiCategory)​

        -- ADDON:RegisterContentTriggerFunc(uiCategory)​ 
___
Important! Many buttons images have been changed in 9.5 and new elements have been added.

For example: default button style.
Before: ApplyButtonSkin(okButton, buttonskin)
After: okButton:SetStyle("text_default")

All available styles you can find in ui/setting/button_style.g

___
Attached is full scripts folder from 9.5. It may help you learn how many things in the interface work
 - 첨부파일 scriptsorig.zp

Applied update:
Returned back fire of UNIT_ENTERED_SIGHT and UNIT_LEAVED_SIGHT events

Enabled functions:
   ADDON:GetAddonInfos()
   ADDON:SetAddonEnable(name, enable)
   ADDON:SaveAddonInfos()
   ADDON:FireAddon(name)
   ADDON:ReloadAddon(name) 

Applied update:
improved ADDON:AddEscMenuButton (you can now set custom image for button) added one more hidden param array of path, x, y, w, h 

example: X2:AddEscMenuButton(5,1000,"manager_icon_esc",X2Locale:LocalizeUiText(INFOBAR_MENU_TEXT, "uiaddon"), {path="addon/manager/manager_icon.dds",x=75,y=0,w=15,h=15})
default one work as before X2:AddEscMenuButton(5,1000,"tgos",X2Locale:LocalizeUiText(INFOBAR_MENU_TEXT, "uiaddon"))

9.5 API types, UI functions, API functions and Console Variables:

Updates 7/1:

    Camera settings for addons:
        changed minimum of custom_zoom_sensitivity to 0.01​
        changed maximum of custom_camera_max_dist to 2000​

    Added new functions:
        ADDON:GetName() - return current addon name​
        ADDON:LoadData(key) - return table of saved data by key​
        ADDON:SaveData(key, table) - save table by key​
        ADDON:ClearData(key) - remove data from save by key​
        key - string, can be same in different addons, it will not overlap​



Updates 7/8:
Fixed memory damage and disappear keys on load data, removed double read.
ADDON:LoadData(key)
ADDON:SaveData(key, table)
ADDON:ClearData(key)

Updates 7/15:
Changed function X2Uint:GetUnitWorldPositionByTarget(unit, isLocal)
    return x,y,z and angle(from 0 to 2xPI)​


Updates 8/12:
Enabled functions with 500ms cooldown:
    X2Auction:GetLowestPrice(itemType, itemGrade)​
    X2Auction:AskMarketPrice(itemType, itemGrade, askMarketPriceUi)​


Update 8/19:

    Enabled functions:​
        X2Hotkey:GetOptionBinding(action, index, option, arg)​
        X2Hotkey:GetOptionBindingButton(buttonName, index)​
        X2Hotkey:SetOptionBindingWithIndex(action, key, index, arg)​
        X2Hotkey:SetOptionBindingButtonWithIndex(buttonName, key)​
        X2Hotkey:EnableHotkey(enable)​
        X2Hotkey:IsOverridableAction(action)​
        X2Hotkey:IsValidActionName(action)​
        X2Skill:Info(skillId)​

    Added new functions:​
        X2Skill:GetCooldown(skillId, ignoreGlobalCooldown) - return cooldown remain and duration.​



Updates 9/2:

    Added new functions and new uievent:​
        X2Hotkey:GetBindingUiEvent(actionName, index)​
        X2Hotkey:SetBindingUiEvent(actionName, key)​
        X2Hotkey:SetBindingUiEventWithIndex(actionName, key, index)​
        X2Hotkey:GetOptionBindingUiEvent(actionName, index)​
        X2Hotkey:SetOptionBindingUiEvent(actionName, key)​
        X2Hotkey:SetOptionBindingUiEventWithIndex(actionName, key)​
        HOTKEY_ACTION ui event notify action name and hotkey is released state​

    Enabled functions:​
        X2Ability:IsActiveAbility(index)​
        X2Ability:GetActiveAbility()​
        X2Ability:GetMyActabilityInfo(actAbilityGroupType)​


Update 9/16:

    Added new functions:​
        X2Skill:GetMateCooldown(skillId, ignoreGlobalCooldown, mateType) - mate types: 0(none)/1(ride)/2(battle)​

    Enabled functions:​
        X2Hotkey:SaveHotKey()​


Update 10/7:

Enabled Functions:

    X2Hotkey:BindingToOption()
    X2Hotkey:OptionToBinding()


Update 11/4:
Added new function X2Map:ShowWorldmapLocation(zoneId, x, y, z)

Update 11/11:
Changed X2Map:ShowWorldmapLocation(zoneGroupId, x, y, z), now it's zone group id and global coordinates.​

Update 12/2:
Enabled functions:​

    X2Equipment:PickupMateEquippedItem(targetName, slotNo)​
    X2Equipment:MateUnequipItem(targetName, slotNo)

Update 2/17:

Enabled functions:

    X2Resident:RefreshResidentMembers(zoneGroupType, bool, int)
    X2Resident:GetResidentMembers(zoneGroupType, bool, int)

Update 2/24:

    Enabled functions:​
        X2Team:MoveTeamMember(frommemberIndex, tomemberIndex) - cooldown 1s​
        X2Team:MoveTeamMemberToParty(frommemberIndex, toParty) - cooldown 1s​
        X2Team:KickTeamMember(memberIndex, teamRoleType) - cooldown 1s
        X2Team:KickTeamMemberByName(charName, teamRoleType) - cooldown 1s
        X2Team:GetTeamRoleType()​


Update 3/24:

Enabled functions:
    X2Unit:SetOverHeadMarker(unit, markerIndex) -- cooldown 1s​
    X2Unit:RemoveAllOverHeadMarker() -- cooldown 1s​
    X2Unit:GetOverHeadMarker(unit)​
    X2Unit:GetOverHeadMarkerUnitId(markerIndex)​

Update 3/31:

-- Enabled functions:
    X2Quest:IsReadyForCompleteQuest(questType)

-- Added new function:
    X2:UpdateEscMenuButton(uiContentType, buttonValue, colorKey)

-- Changed function:
    X2:AddEscMenuButton(categoryId, uiContentType, iconKey, name)

-- Added second additional array with config of button type, values:
    buttonType - 1 normal, 2 on/off, 3 with numbers
    buttonValue - 0 disable, 1 and more enable or show
    colorKey - situation_01/situation_02/situation_03

-- Some examples:
X2:AddEscMenuButton(5,1000,"tgos",X2Locale:LocalizeUiText(INFOBAR_MENU_TEXT, "uiaddon")) - add standard button with default texture
X2:AddEscMenuButton(5,1001,"manager_icon_esc",X2Locale:LocalizeUiText(INFOBAR_MENU_TEXT, "uiaddon"), {path="addon/manager/manager_icon.dds",x=75,y=0,w=15,h=15}) - add button with custom texture
X2:AddEscMenuButton(5,1002,"tgos",X2Locale:LocalizeUiText(INFOBAR_MENU_TEXT, "uiaddon"),{},{buttonType=2,buttonValue=1,colorKey=""}) - add on/off but with on state
X2:AddEscMenuButton(5,1003,"tgos",X2Locale:LocalizeUiText(INFOBAR_MENU_TEXT, "uiaddon"),{},{buttonType=3,buttonValue=2,colorKey="situation_01"}) - add num button with value 2 and color 1

X2:UpdateEscMenuButton(1002,0,"") - set button state to off
X2:UpdateEscMenuButton(1003,1,"situation_01") - change button value to 1
X2:UpdateEscMenuButton(1003,1,"situation_02") - change background color


Update 4/7:

Enabled functions:
    X2Team:RaidRecruitAdd(type, subType, headcount, limitLevel, autoJoin, msg, hour, minute, limitGearPoint) -- cooldown 5sec​
    X2Team:RaidRecruitDel() -- cooldown 5sec​
    X2Bag:GetBagItemInfo(slot)​
    X2Bag:CountItems()​
    X2Bag:ItemStack(slot)​
    X2Bank:GetBagItemInfo(slot)​
    X2Bank:CountItems()​
    X2Bank:ItemStack(slot)​
    X2GuildBank:GetBagItemInfo(slot)​
    X2GuildBank:CountItems()​
    X2GuildBank:ItemStack(slot)​
    X2Coffer:GetBagItemInfo(slot)​
    X2Coffer:CountItems()​
    X2Coffer:ItemStack(slot)​


Update 4/14:

Fixed functions:

    X2Bank:GetBagItemInfo(slot)
    X2GuildBank:GetBagItemInfo(slot)
    X2Coffer:GetBagItemInfo(slot)

Enabled functions:

    X2Team:GetRaidRecruitTypeList()
    X2Team:GetRaidRecruitSubTypeList(recruitTypeList, recruitSubType, bExceptSiege)
    X2Team:GetRaidRecruitSubType(recruitType, recruitSubType)

Changed function:

    X2Team:RaidRecruitAdd(type, subType, headcount, limitLevel, autoJoin, msg, hour, minute, limitGearPoint)
        now headcount limited to 50.



Update 4/28:

Enabled many X2EquipSlotReinforce addon functions:

    GetMaterialInfo(equipSlotIndex, level)
    GetReinforceInfo(equipSlotIndex)
    GetAttributeType(equipSlotIndex)
    IsFullExp(equipSlotIndex)
    GetSetEffects(attributeType)
    GetSetEffectTopLevel(attributeType)
    GetAttributeTotalLevel(attributeType)
    GetNextSetApplyLevel(attributeType)
    GetLevelEffectChangeUIInfo(equipSlotIndex)
    GetLevelEffectStep(equipSlotIndex)
    GetLevelEffectInfoByEquipSlot(equipSlotIndex)
    HasNextSetEffect(attributeType)
    IsInGameShopItemTag(itemType)
    SuitableLevelForEquipSlotReinforce()
    GetAppliedAllLevelEffect()
    GetAppliedAllSetEffect()
    GetAppliedAllBundleEffect()
    GetTotalReinforceLevel()
    GetBundleEffectTopLevel()
    GetBundleEffectInfos()

Additionally enabled functions:

    X2Friend:IsMyFriend(charName)
    X2Friend:GetFriendList(allMember)
    X2Family:GetMemberList(allMember)
    X2Faction:IsMyExpeditionMember(charName)


Update 5/12:

Enabled addon functions:

    X2Bag:MoveToEmptyBankSlot(slotIdx) - cooldown 500ms​
    X2Bag:MoveToEmptyCofferSlot(slotIdx) - cooldown 500ms​
    X2Coffer:MoveToEmptyBagSlot(slotIdx) - cooldown 500ms​
    X2Bank:MoveToEmptyBagSlot(slotIdx) - cooldown 500ms​
    X2Bag:Capacity()​
    X2Coffer:Capacity()​
    X2Bank:Capacity()​
    X2Input:IsShiftKeyDown()​
    X2Input:IsControlKeyDown()​
    X2Input:IsAltKeyDown()​
    X2HotKey:RemoveOptionBinding(action, index, arg)​

Fixed functions that caused client crashes:​

    X2EquipSlotReinforce:GetLevelEffectChangeUIInfo(equipSlotIndex)​
    X2EquipSlotReinforce:GetMaterialInfo(equipSlotIndex, level)​


Update 5/19:

Fixed addon functions restricting use with closed bank or chest window; reduced cooldown to 200ms:​

    X2Bag:MoveToEmptyBankSlot(slotIdx)​
    X2Bag:MoveToEmptyCofferSlot(slotIdx)​
    X2Coffer:MoveToEmptyBagSlot(slotIdx)​
    X2Bank:MoveToEmptyBagSlot(slotIdx)​

Enabled functions:​

    X2BattleField:GetInstanceUiKindList()​
    X2BattleField:GetInstanceListByKind(kind)，​
    X2BattleField:GetDetailInstanceInfo(instanceType)​
    X2BattleField:GetInstanceName(instanceType)​
    X2Squad:CreateSquad(selectedField, openType, explanation, partyInvitation, limitLevel, limitGearScore) - cooldown 5s​

Update 5/26:

Enabled function X2World:GetCurrentWorldName()​
Added new info in GetBagItemInfo for X2Bag, X2Bank, and X2Coffer - "category_id"​


Update 6/2:

Fixed X2Craft:GetCraftMaterialInfo(craftType. Will no longer crash the game client.​
Fixed X2Bag:MoveToEmptyBankSlot(slotIdx), X2Bank:MoveToEmptyBagSlot(slotIdx) that sometimes didn't work.​

Enabled functions:​
    X2Unit:UnitInfo(unit)​
    X2Unit:UnitModifierInfo(unit)​

Update 6/3:

Additional fix for X2Craft:GetCraftMaterialInfo(craftType). Should no longer crash the game client.​

Limited functions:
X2Unit:UnitInfo(unit)​ - player only
X2Unit:UnitModifierInfo(unit) - player only
UnitHiddenBuff(unit, buffIndex) - any except target and ranker
UnitHiddenBuffCount(unit) - any except target and ranker
UnitHiddenBuffTooltip(unit, buffIndex) - any except target and ranker
UnitRemovableDebuffTooltip(unit, buffIndex) - any except target and ranker
UnitRemovableDebuff(unit, buffIndex) - any except target and ranker
UnitRemovableDebuffCount(unit) - any except target and ranker

Update 6/9:

Enabled functions:​
    X2Craft:GetCraftTypeByItemType(itemType)​
    X2Player: PlayerInCombat()​

Limited functions - can't be used in combat:​
    X2Player:ChangeAppellation(nameType, effectType)
    X2Bag:EquipBagItem(slot, isAuxEquip)​

Reduced cooldown for X2Player:ChangeAppellation(nameType, effectType) from 5s to 2s.​
Added RegisterContentWidget for UIC_COFFER, UIC_BANK and UIC_AUCTION.​
Removed UNIT_ENTERED_SIGHT and UNIT_LEAVED_SIGHT events.​

Added new function:​
    X2Unit:GetUnitsInSight(unitOwner) - any - -1
    UO_CHARACTER = 0,
    UO_NPC = 1,
    UO_SLAVE = 2,
    UO_HOUSING = 3,
    UO_TRANSFER = 4,
    UO_MATE = 5,
    UO_SHIPYARD = 6,
    UO_BUTLER = 7​