# 콘솔 변수: 물리

이 목록은 클라이언트 엔진 스냅샷이며 **애드온 허용 API 목록이 아닙니다**.

> ⚠️ 모든 항목을 `지원 외/확인 필요`로 분류합니다. `READONLY`, `REQUIRE_NET_SYNC`, `SAVEGAME` 여부가 개별 항목에 기록되지 않았으므로 변경하지 말고 조사 참고용으로만 사용하세요.

| 변수 | 형식 | 당시 값 | 한국어 설명 | 원본 도움말 | 상태/제약 |
|---|---|---|---|---|---|
| `rope_skill_controller_cut_angvel` | float | `25.1327` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_wireframe_distance` | float | `5` | 물리 관련 엔진 설정값입니다. | Maximum distance at which wireframe is drawn on physics helpers | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_fadeout_time` | float | `3000` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_ray_fadein` | float | `0.2` | 물리 관련 엔진 설정값입니다. | Fade-in time for ray physics helpers | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_net_minsnapdist` | float | `0.1` | 물리 관련 엔진 설정값입니다. | Minimum distance between server position and client position at which to start snapping | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_velocity` | float | `100` | 물리 관련 엔진 설정값입니다. | Clamps physicalized objects' velocities to this value | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_count_objects` | int | `0` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_substeps` | int | `5` | 물리 관련 엔진 설정값입니다. | Limits the number of substeps allowed in variable time step mode. Usage: p_max_substeps 5 Objects that are not allowed to perform time steps beyond some value make several substeps. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_notify_epsilon_rigid` | float | `0.001` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_single_step_mode` | int | `0` | 물리 관련 엔진 설정값입니다. | Toggles physics system 'single step' mode.Usage: p_single_step_mode [0/1] Default is 0 (off). Set to 1 to switch physics system (except players) to single step mode. Each step must be explicitly requested with a 'p_do_step' instruction. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_object_splashes` | int | `3` | 물리 관련 엔진 설정값입니다. | Specifies how many splash events one entity is allowed to generate | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_accuracy_MC` | float | `0.005` | 물리 관련 엔진 설정값입니다. | Desired accuracy of microcontact solver (velocity-related, m/s) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_maxforce` | float | `200000` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_swing_force` | float | `1` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_damping_group_size` | int | `8` | 물리 관련 엔진 설정값입니다. | Sets contacting objects group size before group damping is used.Usage: p_damping_group_size 3 Used for internal tweaking only. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_stiffness` | float | `0` | 물리 관련 엔진 설정값입니다. | stiffness for stretching | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_splash_force0` | float | `10` | 물리 관련 엔진 설정값입니다. | Minimum water hit force to generate splash events at p_splash_dist0 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_splash_force1` | float | `100` | 물리 관련 엔진 설정값입니다. | Minimum water hit force to generate splash events at p_splash_dist1 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_fixed_timestep` | float | `0` | 물리 관련 엔진 설정값입니다. | Toggles fixed time step mode.Usage: p_fixed_timestep [0/1] Forces fixed time step when set to 1. When set to 0, the time step is variable, based on the frame rate. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_characterik` | int | `1` | 물리 관련 엔진 설정값입니다. | Toggles character IK. Usage: p_characterik [0/1] Default is 1 (on). Set to 0 to disable inverse kinematics. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_enforce_contacts` | int | `1` | 물리 관련 엔진 설정값입니다. | This variable is obsolete. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_plane_contacts_distress` | int | `4` | 물리 관련 엔진 설정값입니다. | Same as p_max_plane_contacts, but is effective if total number of contacts is above p_max_contacts | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_contact_gap_player` | float | `0.01` | 물리 관련 엔진 설정값입니다. | Sets the safe contact gap for player collisions with the physical environment.Usage: p_max_contact_gap_player 0.01 This variable is used for internal tweaking only. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_prohibit_unprojection` | int | `1` | 물리 관련 엔진 설정값입니다. | Prohibit Unprojection and velocity penalties upon Unprojection | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_contact_gap_simple` | float | `0.03` | 물리 관련 엔진 설정값입니다. | Specifies the maximum contact gap for objects that use the simple solver | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_list_active_objects` | int | `0` | 물리 관련 엔진 설정값입니다. | 0: off 1: list once 2: on | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_LCPCG_subiters` | int | `120` | 물리 관련 엔진 설정값입니다. | Limits the number of LCP CG solver inner iterations (should be of the order of the number of contacts) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_noGeomLoad` | int | `0` | 물리 관련 엔진 설정값입니다. | do not load or create geometry | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_mass_decay_attached_scale` | float | `100` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_num_bodies_large_group` | int | `100` | 물리 관련 엔진 설정값입니다. | Group size to be used with p_max_substeps_large_group, in bodies | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_accuracy_LCPCG_no_improvement` | float | `0.05` | 물리 관련 엔진 설정값입니다. | Required LCP CG accuracy that allows to stop if there was no improvement after p_max_LCPCG_fruitless_iters | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_cut_vel` | float | `30` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_players_can_break` | int | `0` | 물리 관련 엔진 설정값입니다. | Whether living entities are allowed to break static objects with breakable joints | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_penalty_scale` | float | `0.3` | 물리 관련 엔진 설정값입니다. | Scales the penalty impulse for objects that use the simple solver | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_relative_waterlevel_for_change_to_flymode` | float | `1` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_splash_vel0` | float | `4.5` | 물리 관련 엔진 설정값입니다. | Minimum water hit velocity to generate splash events at p_splash_dist0 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_splash_vel1` | float | `10` | 물리 관련 엔진 설정값입니다. | Minimum water hit velocity to generate splash events at p_splash_dist1 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_drawPrimitives` | int | `0` | 물리 관련 엔진 설정값입니다. | should be used with p_draw_helpers. colorizing primitives | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_net_velsnapmul` | float | `0.1` | 물리 관련 엔진 설정값입니다. | Multiplier to expand the p_net_minsnapdist based on the objects velocity | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_break_on_validation` | int | `0` | 물리 관련 엔진 설정값입니다. | Toggles break on validation error. Usage: p_break_on_validation [0/1] Default is 0 (off). Issues DebugBreak() call in case of a physics parameter validation error. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_finish_accel_velocity` | float | `5` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_LCPCG_microiters_final` | int | `25000` | 물리 관련 엔진 설정값입니다. | Same as p_max_LCPCG_microiters, but for the final LCP CG iteration | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_contacts` | int | `150` | 물리 관련 엔진 설정값입니다. | Maximum contact number, after which contact reduction mode is activated | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_joint_dmg_accum_thresh` | float | `0` | 물리 관련 엔진 설정값입니다. | If set, joint damage over this threshold will be accumulated | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_group_damping` | float | `0.5` | 물리 관련 엔진 설정값입니다. | Toggles damping for object groups. Usage: p_group_damping [0/1] Default is 1 (on). Used for internal tweaking only. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_joint_dmg_accum` | float | `0` | 물리 관련 엔진 설정값입니다. | If set, joint damage will be accumulated | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_skip_redundant_colldet` | int | `1` | 물리 관련 엔진 설정값입니다. | Specifies whether to skip furher collision checks between two convex objects using the simple solver when they have enough contacts between them | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_notify_epsilon_living` | float | `0.001` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_contact_gap` | float | `0.01` | 물리 관련 엔진 설정값입니다. | Sets the gap, enforced whenever possible, between contacting physical objects.Usage: p_max_contact_gap 0.01 This variable is used for internal tweaking only. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_net_angsnapmul` | float | `0.01` | 물리 관련 엔진 설정값입니다. | Multiplier to expand the p_net_minsnapdot based on the objects angular velocity | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_jump_velocity` | float | `8` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_debris_mass` | float | `10` | 물리 관련 엔진 설정값입니다. | Broken pieces with mass<=this limit use debris collision settings | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_world_step` | float | `0.2` | 물리 관련 엔진 설정값입니다. | Specifies the maximum step physical world can make (larger steps will be truncated) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_use_distance_contacts` | int | `0` | 물리 관련 엔진 설정값입니다. | Allows to use distance-based contacts (is forced off in multiplayer) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_time_granularity` | float | `0.0001` | 물리 관련 엔진 설정값입니다. | Sets physical time step granularity. Usage: p_time_granularity [0..0.1] Used for internal tweaking only. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_unproj_vel` | float | `2.5` | 물리 관련 엔진 설정값입니다. | Limits the maximum unprojection velocity request | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_pod_life_time` | float | `8` | 물리 관련 엔진 설정값입니다. | How long POD(Physicalized On Demand. Vegetation) will keep alive | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_net_smoothtime` | float | `5` | 물리 관련 엔진 설정값입니다. | How much time should non-snapped positions take to synchronize completely? | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_max_timestep` | float | `0` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_plane_contacts` | int | `8` | 물리 관련 엔진 설정값입니다. | Maximum number of contacts lying in one plane between two rigid bodies (the system tries to remove the least important contacts to get to this value) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_unproj_vel_scale` | float | `10` | 물리 관련 엔진 설정값입니다. | Requested unprojection velocity is set equal to penetration depth multiplied by this number | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_max_safe_step` | float | `0` | 물리 관련 엔진 설정값입니다. | if a segment stretches more than this (in *relative* units), its length is reinforced | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_tick_breakable` | float | `0.1` | 물리 관련 엔진 설정값입니다. | Sets the breakable objects structure update interval | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_lattice_max_iters` | int | `100000` | 물리 관련 엔진 설정값입니다. | Limits the number of iterations of lattice tension solver | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_air_resistance` | float | `0` | 물리 관련 엔진 설정값입니다. | "advanced" (more correct) version of damping | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_LCPCG_contacts` | int | `100` | 물리 관련 엔진 설정값입니다. | Maximum number of contacts that LCPCG solver is allowed to handle | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_min_separation_speed` | float | `0.02` | 물리 관련 엔진 설정값입니다. | Used a threshold in some places (namely, to determine when a particle goes to rest, and a sliding condition in microcontact solver) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_entity_cells` | int | `300000` | 물리 관련 엔진 설정값입니다. | Limits the number of entity grid cells an entity can occupy | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_accuracy_LCPCG` | float | `0.005` | 물리 관련 엔진 설정값입니다. | Desired accuracy of LCP CG solver (velocity-related, m/s) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_approx_caps` | int | `7` | 물리 관련 엔진 설정값입니다. | Maximum number of capsule approximation levels for breakable trees | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_ray_on_grid_max_size` | int | `512` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_player_velocity` | float | `150` | 물리 관련 엔진 설정값입니다. | Clamps players' velocities to this value | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_stiffness_norm` | float | `0` | 물리 관련 엔진 설정값입니다. | stiffness for shape preservation along normals ("convexity preservation") | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_stiffness_tang` | float | `0` | 물리 관련 엔진 설정값입니다. | stiffness for shape preservation against tilting | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_friction` | float | `0` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_approx_caps_len` | float | `1.2` | 물리 관련 엔진 설정값입니다. | Breakable trees are approximated with capsules of this length (0 disables approximation) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_GEB_max_cells` | int | `800` | 물리 관련 엔진 설정값입니다. | Specifies the cell number threshold after which GetEntitiesInBox issues a warning | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_list_objects` | int | `0` | 물리 관련 엔진 설정값입니다. | 0: off 1: list once 2: on | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_MC_mass_ratio` | float | `100` | 물리 관련 엔진 설정값입니다. | Maximum mass ratio between objects in an island that MC solver is considered safe to handle | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_substeps_large_group` | int | `5` | 물리 관련 엔진 설정값입니다. | Limits the number of substeps large groups of objects can make | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_min_LCPCG_improvement` | float | `0.05` | 물리 관련 엔진 설정값입니다. | Defines a required residual squared length improvement, in fractions of 1 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_LCPCG_iters` | int | `5` | 물리 관련 엔진 설정값입니다. | Maximum number of LCP CG iterations | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_LCPCG_microiters` | int | `12000` | 물리 관련 엔진 설정값입니다. | Limits the total number of per-contact iterations during one LCP CG iteration (number of microiters = number of subiters * number of contacts) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_limit_simple_solver_energy` | int | `1` | 물리 관련 엔진 설정값입니다. | Specifies whether the energy added by the simple solver is limited (0 or 1) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_mass_decay` | float | `1` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_use_unproj_vel` | int | `0` | 물리 관련 엔진 설정값입니다. | internal solver tweak | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_do_step` | int | `0` | 물리 관련 엔진 설정값입니다. | Steps physics system forward when in single step mode. Usage: p_do_step 1 Default is 0 (off). Each 'p_do_step 1' instruction allows the physics system to advance a single step. | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_target_moved_away_dist` | float | `2` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_cull_distance` | float | `100` | 물리 관련 엔진 설정값입니다. | Culling distance for physics helpers rendering | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_thickness` | float | `0` | 물리 관련 엔진 설정값입니다. | thickness for collision checks | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `cloth_damping` | float | `0` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_draw_helpers` | string | `0` | 물리 관련 엔진 설정값입니다. | Same as p_draw_helpers_num, but encoded in letters Usage [Entity_Types]_[Helper_Types] - [t\|s\|r\|R\|l\|i\|g\|a\|y\|e]_[g\|c\|b\|l\|t(#)] Entity Types: t - show terrain s - show static entities r - show sleeping rigid bodies R - show active rigid bodies l - show living entities i - show independent entities g - show triggers a - show areas y - show rays in RayWorldIntersection e - show explosion occlusion maps Helper Types g - show geometry c - show contact points b - show bounding boxes l - show tetrahedra lattices for breakable objects j - show structural joints (will force translucency on the main geometry) t(#) - show bounding volume trees up to the level # f(#) - only show geometries with this bit flag set (multiple f's stack) Example: p_draw_helpers larRis_g - show geometry for static, sleeping, active, independent entities and areas | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_MC_vel` | float | `15` | 물리 관련 엔진 설정값입니다. | Maximum object velocity in an island that MC solver is considered safe to handle | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_draw_helpers_num` | int | `0` | 물리 관련 엔진 설정값입니다. | Toggles display of various physical helpers. The value is a bitmask: bit 0  - show contact points bit 1  - show physical geometry bit 8  - show helpers for static objects bit 9  - show helpers for sleeping physicalized objects (rigid bodies, ragdolls) bit 10 - show helpers for active physicalized objects bit 11 - show helpers for players bit 12 - show helpers for independent entities (alive physical skeletons,particles,ropes) bits 16-31 - level of bounding volume trees to display (if 0, it just shows geometry) Examples: show static objects - 258, show active rigid bodies - 1026, show players - 2050 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_skill_controller_air_time_for_change_to_flymode` | float | `0.25` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_MC_iters` | int | `6000` | 물리 관련 엔진 설정값입니다. | Specifies the maximum number of microcontact solver iterations | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_net_minsnapdot` | float | `0.99` | 물리 관련 엔진 설정값입니다. | Minimum quat dot product between server orientation and client orientation at which to start snapping | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `rope_max_allowed_step` | float | `0.02` | 물리 관련 엔진 설정값입니다. | 설명 없음 | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_LCPCG_subiters_final` | int | `250` | 물리 관련 엔진 설정값입니다. | Limits the number of LCP CG solver inner iterations during the final iteration (should be of the order of the number of contacts) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_ray_peak_time` | float | `0` | 물리 관련 엔진 설정값입니다. | Rays that take longer then this (in ms) will use different color | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_max_LCPCG_fruitless_iters` | int | `4` | 물리 관련 엔진 설정값입니다. | Maximum number of LCP CG iterations w/o improvement (defined by p_min_LCPCGimprovement) | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_splash_dist0` | float | `7` | 물리 관련 엔진 설정값입니다. | Range start for splash event distance culling | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
| `p_splash_dist1` | float | `30` | 물리 관련 엔진 설정값입니다. | Range end for splash event distance culling | ⚠️ 애드온 API 아님 / 변경 금지 권장 |
